import { exampleSdkStackexchange } from './example-sdk-stackexchange';

describe('exampleSdkStackexchange', () => {
  it('should work', () => {
    expect(exampleSdkStackexchange()).toEqual('example-sdk-stackexchange');
  });
});
